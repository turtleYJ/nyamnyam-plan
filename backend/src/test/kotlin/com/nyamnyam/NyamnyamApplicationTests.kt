package com.nyamnyam

import org.junit.jupiter.api.Test

class NyamnyamApplicationTests {

    @Test
    fun contextLoads() {
        // Application context load test
        // Full context load requires DB connection, so kept minimal for now
    }
}
